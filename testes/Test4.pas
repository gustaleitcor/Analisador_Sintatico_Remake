program Test4;
var
    A, R, BC, I : integer;
    O, U: real;
    V, OLA: boolean;

procedure OLA();
var A: integer;
begin end;

begin
    while (I <= 5) do
    begin
        TESTE();

        A := A+1;

        A := (C + A + UI + 1/21) + I(A, B, C +A, 303);
    end
end.